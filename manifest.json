{
  "manifest_version": "0.3",
  "name": "figma-fast",
  "display_name": "Figma Fast",
  "version": "0.1.0",
  "description": "High-performance MCP bridge to Figma — read, edit, and build designs directly from Claude.",
  "author": {
    "name": "Jarod"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/JarodXP/figma-fast"
  },
  "license": "MIT",
  "server": {
    "type": "node",
    "entry_point": "server/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/server/index.js"],
      "env": {
        "FIGMA_TOKEN": "${user_config.figma_token}"
      }
    }
  },
  "user_config": {
    "figma_token": {
      "type": "string",
      "title": "Figma Personal Access Token",
      "description": "Required to read comments. Generate one at figma.com → Account Settings → Personal access tokens.",
      "required": false,
      "sensitive": true
    }
  },
  "tools_generated": true
}
